version: '2'
services:
    noseservice-postgresql:
        image: postgres:9.6.2
        # volumes:
        #     - ~/volumes/jhipster/noseService/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=noseService
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
